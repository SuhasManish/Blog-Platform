# Local development — API on port 5000
REACT_APP_API_URL=http://localhost:5000

# Production single-server deploy (build with empty URL = same origin)
# REACT_APP_API_URL=

# Production split deploy (API on another host)
# REACT_APP_API_URL=https://your-api.onrender.com
